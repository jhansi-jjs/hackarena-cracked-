# TrustRAG — Team Cracked

Privacy-first local AI agent with 10 guardrails.
Built with: llamafile, encoderfile, mcpd, any-agent, Flask

## Run
1. Start llamafile, encoderfile, mcpd
2. `pip install flask any-agent mcpd`
3. `python3 app.py`
4. Open http://localhost:5050
